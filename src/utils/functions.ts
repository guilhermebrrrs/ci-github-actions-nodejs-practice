export const somar = (a: number, b: number) => a + b;

export const subtrair = (a: number, b: number) => a - b;

export const multiplicar = (a: number, b: number) => a * b;

export const dividir = (a: number, b: number) => a / b;

export const dividir2 = (ab: number, bb: number) => ab / bb;

export const dividir3 = (ac: number, bc: number) => ac / bc;

export const dividir4 = (ad: number, bd: number) => ad / bd;

export const dividir5 = (ae: number, be: number) => ae / be;
